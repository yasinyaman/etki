"""Plugin management CLI (`python -m etki.plugin`) and, in later phases, the
installer/lockfile/marketplace client. Lives OUTSIDE `etki/engine` so plugin
work never collides with the answer-key freeze guard."""
